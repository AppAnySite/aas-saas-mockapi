######################################################################
# aas-saas-mockapi.compose.yml                                      #
#                                                                    #
# Docker Compose for MockAPI Microservice                            #
#                                                                    #
# Services:                                                          #
#   - aas-saas-mockapi: Provides mock API endpoints for              #
#     the AppAnySite SaaS platform.                                  #
#                                                                    #
# Includes build args, healthchecks, resource limits, and networks.  #
#                                                                    #
# Maintainer: Maigha Inc DevOps Team                                 #
# Authors: 2024 Maigha Inc, AppAnySite Contributors                  #
# License: Proprietary/Confidential                                  #
#                                                                    #
# For questions, contact: devops@maigha.com                          #
######################################################################

x-logging: &default-logging
  driver: "json-file"
  options:
    max-size: "10m"
    max-file: "3"

x-healthcheck: &default-healthcheck
  interval: 30s
  timeout: 10s
  retries: 5

services:
  aas-saas-mockapi:
    image: aas-saas-mockapi:latest
    build:
      context: .
      dockerfile: Dockerfile
    container_name: aas-saas-mockapi
    restart: unless-stopped
    logging: *default-logging
    labels:
      maintainer: "AASTeam"
      com.maighamedia.service: "aas-saas-mockapi"
      prometheus.scrape: "true"
      prometheus.port: "${MOCKAPI_PORT:-3002}"
    deploy:
      resources:
        limits:
          cpus: "0.30"
          memory: "256M"
        reservations:
          cpus: "0.15"
          memory: "128M"
    ports:
      - "${MOCKAPI_PORT:-3002}:${MOCKAPI_PORT:-3002}"
    env_file:
      - .config/env/.env.${NODE_ENV}
    networks:
      - my-network
    healthcheck:
      <<: *default-healthcheck
      test: ["CMD","sh","-c","curl -f http://localhost:${MOCKAPI_PORT:-3002}/health"]
